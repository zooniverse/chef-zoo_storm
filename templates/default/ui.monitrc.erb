check process ui with pidfile <%= @storm_dir %>/ui.pid
  start program = "/usr/sbin/service ui start"
  stop program = "/usr/sbin/service ui stop"
  if failed port 8080 then restart
  if 5 restarts within 5 cycles then timeout
